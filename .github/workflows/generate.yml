name: Generate Dynamic HTMLs

on:
  push:
    paths:
      - 'dynamic/data.json'
      - 'dynamic/generate-html.js'
      - 'dynamic/generate-wacust.js'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

    

      - name: Generate wacust/1–30.html
        run: node dynamic/generate-wacust.js

      - name: Commit and Push
        run: |
          git config user.name "OG Meta Bot"
          git config user.email "bot@sellinseconds.in"
          git add dynamic/buygallery.html dynamic/wacust/*.html
          git commit -m "Auto-update dynamic HTMLs from data.json" || echo "No changes to commit"
          git push